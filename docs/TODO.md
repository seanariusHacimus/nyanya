# Отложенное

То, что осознанно не сделано и к чему нужно вернуться. Фазы разработки — в
`BACKEND-PLAN.md`, здесь только хвосты.

## Отложено по решению владельца

| Что | Состояние | Что сделать |
|---|---|---|
| `logos-source/` | 14 МБ исходных файлов фирменного стиля лежат в корне репозитория вне git (untracked). Это оригиналы логотипа: вордмарк, знак доверия, иконка заботы, горизонтальный локап, версия на тёмном. В репозитории есть только `public/nyanya-logo*.png` — производные | Решить: коммитить в репозиторий (плюс 14 МБ в историю), вынести в отдельное хранилище или в Git LFS. До тех пор файлы существуют в одном экземпляре и теряются вместе с папкой |
| Документы демо-анкет | У двенадцати анкет из первоначального набора загружены файлы-заглушки: валидные PDF с пометкой «DEMO DATA — NOT A REAL DOCUMENT». Фотографии настоящие | Заменить настоящими сканами до публичного запуска. Пока `nyanya.uz` не подключён, это допустимо |

## Блокирует запуск

| Что | Состояние |
|---|---|
| `BETTER_AUTH_SECRET` | В production стоит незаменённый плейсхолдер — сессии подписаны публично известной строкой |
| ~~Домен отправки писем~~ | ✅ Решено 2026-08-04: домен `nyanya.uz` подтверждён в Resend, `EMAIL_FROM` = `NYANYA.UZ <noreply@nyanya.uz>`. Доставка проверена на трёх адресах |
| `nyanya.uz` | Не подключён, сервис доступен по адресу Railway |
| Локализация данных (ЗРУ-547) | Документы верификации хранятся в Амстердаме. В `nyanya_project.md` это названо условием запуска |
| Узбекский и английский | Переключатель языков есть, переводов нет. В плане фаз локализация не значится |

## Технический долг

| Что | Состояние |
|---|---|
| Восстановление пароля | Не реализовано. Раньше упиралось в почту — теперь почта работает, так что можно делать |
| Ветка `main` | Осталась на старом приложении (последний коммит — 2026-07-08). Либо привести в соответствие с `master`, либо удалить |
| Предупреждение Turbopack | `next.config.ts` использует `path.join(__dirname)` для фиксации корня, из-за чего трассировка файлов ругается. Безвредно, но шумит в каждой сборке |
